version: 1
frontend:
  phases:
    build:
      commands:
        - pip install -r requirements.txt
    start:
      commands:
        - python main.py
  artifacts:
    baseDirectory: /
    files:
      - '**/*'